# Arch Linux ARM PKGBUILDs for Pinebook Pro

Herein lie the PKGBUILDs for the [simulated.earth](https://simulated.earth/archlinux/pinebookpro/aarch64) repo.

Add the following to */etc/pacman.conf*
```
[pinebookpro]
SigLevel = Optional TrustAll
Server = https://simulated.earth/archlinux/$repo/$arch
```
